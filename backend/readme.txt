
transaction -> json -> mine_block -> on_chain


Block's data is an array type, and the genesis block's data is an empty array.

hash's value, must be 0...9, a...f


export PEER=True && python3 -m backend.app

